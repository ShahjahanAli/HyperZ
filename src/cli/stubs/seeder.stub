import { Database } from '../../src/database/Database.js';

export async function run(): Promise<void> {
  const manager = Database.getEntityManager();

  // Add your seed data here
  await manager.insert('{{tableName}}', [
    // { name: 'Example', created_at: new Date(), updated_at: new Date() },
  ]);
}
